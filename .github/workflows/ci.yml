# =================================================
# ZAPP-WEB CI/CD Pipeline
# =================================================
# Triggered on: push to main, pull requests
# Uses Bun (the project ships a bun.lock, not package-lock.json).
# Existing repo debt is reported as warnings until dedicated cleanup PRs land.
# =================================================

name: CI/CD Pipeline

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]
  workflow_dispatch:

concurrency:
  # PR: grupo unico por numero de PR, sem cancelar (auditorias longas nao sao mortas).
  # Push/schedule: grupo por ref, cancela builds intermediarios de main/develop.
  group: ci-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: ${{ github.event.pull_request.number == '' }}

env:
  CI: true
  VITE_SUPABASE_URL: https://example.supabase.co
  VITE_SUPABASE_ANON_KEY: test-anon-key
  VITE_SUPABASE_PUBLISHABLE_KEY: test-anon-key
  VITE_EXTERNAL_SUPABASE_URL: https://example.supabase.co
  VITE_EXTERNAL_SUPABASE_ANON_KEY: test-anon-key

jobs:
  lockfile:
    name: Verify Lockfile
    # Piloto de migração pra ubuntu-latest (auditoria 2026-08-21): o repo está
    # PÚBLICO hoje (confirmado via `gh repo view --json visibility` — a
    # premissa "repo privado + cota billing esgotada" de 2026-08-18 não se
    # aplica mais; repos públicos têm minutos ubuntu-latest gratuitos e
    # ilimitados). Este job foi escolhido como piloto por ser o mais seguro:
    # só git/jq/cat/base64 (nada de bun de fato, apesar do comentário antigo),
    # sem secrets, sem rede além de `git fetch origin` (remoto público).
    # Motivação: os ~48 workflows deste repo competem TODOS pelo mesmo pool
    # de 6 runners self-hosted (vps-zapp) — pushes próximos no tempo (comum
    # neste repo) enfileiram por dezenas de minutos. Se este piloto se provar
    # estável, migrar os demais candidatos sem dependência de rede interna da
    # VPS libera o pool pros jobs que realmente precisam dela (DB live, deploy).
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Verify bun.lock is in sync with package.json
        # bun.lock pins some packages from a private GAR registry that is
        # unavailable on CI, so `bun install --frozen-lockfile` always fails
        # on CI (registry credentials unavailable).  Instead we enforce the
        # invariant with a targeted git diff: only fail when dependency
        # sections change without a corresponding bun.lock update.
        # Script/version/author edits do NOT affect the lockfile and must NOT
        # trigger this check (false positive
        # fixed 2026-07-16 after a scripts-only package.json edit blocked CI).
        run: |
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            git fetch origin ${{ github.base_ref }}
            BASE="origin/${{ github.base_ref }}"
            LOCK_DIFF=$(git diff "$BASE"...HEAD --name-only -- bun.lock)
            OLD_RAW=$(git show "$BASE:package.json" 2>/dev/null || echo '{}')
            if echo "$OLD_RAW" | jq '.' >/dev/null 2>&1; then
              OLD_PKG="$OLD_RAW"
            else
              OLD_PKG=$(echo "$OLD_RAW" | base64 -d 2>/dev/null || echo '{}')
            fi
            NEW_PKG=$(cat package.json)
          else
            LOCK_DIFF=$(git diff HEAD^ HEAD --name-only -- bun.lock 2>/dev/null || true)
            OLD_RAW=$(git show "HEAD^:package.json" 2>/dev/null || echo '{}')
            if echo "$OLD_RAW" | jq '.' >/dev/null 2>&1; then
              OLD_PKG="$OLD_RAW"
            else
              OLD_PKG=$(echo "$OLD_RAW" | base64 -d 2>/dev/null || echo '{}')
            fi
            NEW_PKG=$(cat package.json)
          fi
          # Compare only the dependency fields — scripts/version changes are irrelevant.
          # If OLD_PKG is not valid JSON (e.g. base branch has corrupted content), skip the
          # comparison entirely rather than comparing against a synthetic empty object, which
          # would always appear as a "dep change" and produce a false-positive failure.
          if ! echo "$OLD_PKG" | jq empty 2>/dev/null; then
            echo "::warning::Base package.json could not be parsed as JSON — lockfile diff skipped."
            echo "Lockfile consistency check skipped (base package.json unreadable)."
            exit 0
          fi
          OLD_DEPS=$(echo "$OLD_PKG" | jq -Sc '{d: (.dependencies // {}), dd: (.devDependencies // {}), pd: (.peerDependencies // {}), od: (.optionalDependencies // {})}')
          NEW_DEPS=$(echo "$NEW_PKG" | jq -Sc '{d: (.dependencies // {}), dd: (.devDependencies // {}), pd: (.peerDependencies // {}), od: (.optionalDependencies // {})}')
          echo "Dependency fields changed: $([ "$OLD_DEPS" != "$NEW_DEPS" ] && echo yes || echo no)"
          echo "bun.lock changed:         ${LOCK_DIFF:-none}"
          if [ "$OLD_DEPS" != "$NEW_DEPS" ] && [ -z "$LOCK_DIFF" ]; then
            echo "::error::Dependency fields in package.json changed without updating bun.lock. Run 'bun install' locally and commit the updated lockfile."
            exit 1
          fi
          echo "Lockfile consistency check passed."

  quality:
    name: Quality diagnostics
    # [2026-08-18] self-hosted: repo privado + cota billing esgotada; tools=bun node
    runs-on: [Linux, X64, vps-zapp]
    needs: lockfile
    steps:
      - name: Checkout code
        uses: actions/checkout@v7
        with:
          persist-credentials: false

      - name: Setup Node
        uses: actions/setup-node@v7
        with:
          node-version: 22

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.14

      - name: Cache Bun dependencies
        uses: actions/cache@v6
        with:
          path: ~/.bun/install/cache
          key: bun-${{ runner.os }}-${{ hashFiles('bun.lock') }}
          restore-keys: |
            bun-${{ runner.os }}-

      - name: Install dependencies
        run: bun install

      - name: ESLint diagnostics
        run: |
          set +e
          bunx eslint .
          status=$?
          if [ "$status" -ne 0 ]; then
            echo "::error title=ESLint::ESLint reprovou. Teto: --max-warnings 6 (baseline medido em 2026-08-02)."
          fi
          exit $status

      # P0-2 (A8-seguranca): gate anti-*** — falha se um valor de apikey/Authorization
      # literalmente mascarado com *** reaparecer em src/ ou supabase/functions/.
      # Causa raiz do P0-2: redação de saída em ferramentas de auditoria foi
      # confundida com conteúdo real; este gate garante que NENHUM *** literal
      # entre no código (seja por redação acidental, seja por commit manual).
      - name: Anti-*** literal gate (P0-2 — apikey/Authorization mascarados)
        run: |
          set +e
          HITS=$(grep -rnE 'apikey:[[:space:]]*\*{3}|Authorization:[[:space:]]*\*{3}' src supabase/functions 2>/dev/null)
          if [ -n "$HITS" ]; then
            echo "::error title=Anti-stars gate::apikey/Authorization com valor mascarado *** detectado em src/ ou supabase/functions/:"
            printf '%s\n' "$HITS"
            exit 1
          fi
          echo "Anti-*** gate passed: nenhum apikey/Authorization literal mascarado em src/ supabase/functions/."

      - name: Design-system ratchet (blocking — teto 88, E02/F1-10)
        run: bun run scripts/check-design-system.ts --ci --max=130

      - name: Supabase schema coverage gate (zapp + evo)
        id: schema_gate
        continue-on-error: true
        env:
          SCHEMA_REPORT_DIR: reports/schema-status
          META_URL: ${{ secrets.ZAPP_META_URL }}
          META_TOKEN: ${{ secrets.ZAPP_META_TOKEN }}
        run: node scripts/check-types-schemas.mjs

      - name: Auto-repair types (fallback quando o gate falha)
        id: schema_repair
        if: steps.schema_gate.outcome == 'failure' && env.META_URL != '' && env.META_TOKEN != ''
        continue-on-error: true
        env:
          SCHEMA_REPORT_DIR: reports/schema-status
          META_URL: ${{ secrets.ZAPP_META_URL }}
          META_TOKEN: ${{ secrets.ZAPP_META_TOKEN }}
        run: |
          mkdir -p reports/types-repair
          set +e
          node scripts/repair-types-schemas.mjs 2>&1 | tee reports/types-repair/repair.log
          echo "${PIPESTATUS[0]}" > reports/types-repair/exit-code.txt
          exit 0

      - name: Publish schema status report
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: schema-status-report
          path: reports/schema-status/
          if-no-files-found: warn
          retention-days: 14

      - name: Publish types:repair log
        if: always() && steps.schema_repair.outcome != 'skipped'
        uses: actions/upload-artifact@v7
        with:
          name: types-repair-log
          path: reports/types-repair/
          if-no-files-found: ignore
          retention-days: 14

      - name: Schema gate final (bloqueante)
        env:
          SCHEMA_REPORT_DIR: reports/schema-status
          META_URL: ${{ secrets.ZAPP_META_URL }}
          META_TOKEN: ${{ secrets.ZAPP_META_TOKEN }}
        run: node scripts/check-types-schemas.mjs

      - name: TypeScript check (ratchet, blocking)
        run: node scripts/check-tsc-ratchet.mjs

      - name: TypeScript check — chat components (blocking, strict)
        run: |
          set +e
          OUT=$(bunx tsgo --noEmit -p tsconfig.app.json 2>&1)
          echo "$OUT" | grep -E "src/(features/inbox/components/chat|lib/react-refs)" | grep -E "error TS" && {
            echo "::error title=Chat components typecheck::TypeScript errors detected in chat components (baseline is zero).";
            exit 1;
          }
          exit 0

      - name: ts-nocheck drift gate (blocking)
        run: node scripts/check-ts-nocheck.mjs

  # audit-contract-scan: SEMPRE corre (inclusive em PRs de fork sem secrets).
  # Valida: edge function invoke() tem diretório correspondente + script roda sem
  # erro de sintaxe. Gate bloqueante: test e build dependem deste job.
  audit-contract-scan:
    name: Audit contract (scan-only, always runs)
    # ubuntu-latest (auditoria 2026-08-21, batch 3): scan-only, sem DB — só node
    # local sobre edge functions/estrutura do repo, sem secrets.
    runs-on: ubuntu-latest
    needs: lockfile
    steps:
      - name: Checkout code
        uses: actions/checkout@v7
        with:
          persist-credentials: false

      - name: Setup Node
        uses: actions/setup-node@v7
        with:
          node-version: 22

      - name: Scan contract (sem DB — edge functions + estrutura)
        run: node scripts/audit-contract.mjs --scan-only

  # audit-contract-live: só corre em push/workflow_dispatch (NUNCA em pull_request).
  # Motivo de segurança: o job executa scripts/audit-contract.mjs com
  # SUPABASE_SERVICE_ROLE_KEY. Em eventos pull_request, código não revisado do
  # branch do PR roda com acesso ao secret, criando risco de exfiltração.
  # O scan estrutural (audit-contract-scan) continua disponível para PRs.
  # NÃO está no needs de test/build — é informativo. Para forçar gate no CD,
  # promover para needs quando runner self-hosted com acesso ao DB estiver disponível.
  audit-contract-live:
    name: Audit front↔DB contract (live, requires secrets)
    # [2026-08-18] self-hosted: repo privado + cota billing esgotada; tools=node pg
    runs-on: [Linux, X64, vps-zapp]
    needs: lockfile
    if: github.event_name != 'pull_request'
    # NOTA (fix 2026-08-05): `secrets` NÃO é permitido em `if:` de job no GitHub
    # Actions ("contexts secrets is not allowed here") — isso tornava o workflow
    # INTEIRO inválido (0 jobs, falha silenciosa em 20/20 runs). O condicional
    # agora é avaliado dentro do STEP (env de job é permitido em steps) via guard.
    # Sem o secret, o step é pulado e o job passa (PRs de forks não quebram).
    env:
      SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
      SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
      SUPABASE_DB_URL: ${{ secrets.SUPABASE_DB_URL }}
      # Alternativa (driver pg): DB_URL: ${{ secrets.SUPABASE_DB_URL }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v7
        with:
          persist-credentials: false

      - name: Setup Node
        uses: actions/setup-node@v7
        with:
          node-version: 22

      - name: Install pg driver (modo pg = ground truth pg_catalog)
        run: npm install --no-save --no-audit --no-fund pg@8

      - name: Run contract audit (RPCs, .from(), edge functions vs banco)
        if: ${{ env.SUPABASE_SERVICE_ROLE_KEY != '' }}
        run: node scripts/audit-contract.mjs

  # contract-gate: gate de contrato front↔banco BLOQUEANTE em pull_request
  # (etapa 84 do plano de correção 2026-08-05).
  # Gap fechado: audit-contract-scan valida só estrutura (--scan-only, sem DB —
  # não detecta RPC/.from divergentes) e audit-contract-live nunca roda em PR
  # (único caminho de merge em main). Este job roda o script COMPLETO em PR:
  # divergência (exit 1) falha o job → test/build (needs) são pulados →
  # workflow vermelho → merge bloqueado.
  # Segurança: mesmo guard do audit-contract-live — sem secret o step é pulado
  # e o job passa (PRs de fork não quebram). Exit 2 (env/conexão indisponível)
  # vira warning (fail-open, padrão do repo) para o gate não nascer quebrado
  # (GAP-J da simulação 20260804). Em push o step é pulado (o live cobre).
  contract-gate:
    name: Contract gate (front↔DB, bloqueante em PR)
    # [2026-08-18] self-hosted: repo privado + cota billing esgotada; tools=node pg
    runs-on: [Linux, X64, vps-zapp]
    needs: lockfile
    env:
      SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
      SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
      SUPABASE_DB_URL: ${{ secrets.SUPABASE_DB_URL }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v7
        with:
          persist-credentials: false

      - name: Setup Node
        uses: actions/setup-node@v7
        with:
          node-version: 22

      - name: Install pg driver (modo pg = ground truth pg_catalog)
        run: npm install --no-save --no-audit --no-fund pg@8

      - name: Run audit-contract completo (bloqueante)
        if: github.event_name == 'pull_request'
        run: |
          set +e
          node scripts/audit-contract.mjs
          status=$?
          set -e
          if [ "$status" -eq 2 ]; then
            echo "::warning title=Contract gate::audit-contract sem ambiente de banco (exit 2) — gate pulado nesta run."
            exit 0
          fi
          exit $status

  # migration-gates: gate de migrations (AG-EX-13, fecha os achados A/B da auditoria
  # 2026-08-05 — ANALISE_CLAUDE_EXAUSTIVA.md §1.11). Sem secrets — roda em PR e push.
  # Política implementada em scripts/check-migration-gates.mjs (allowlist via args):
  #   - FAIL em antipadrão `EXCEPTION WHEN OTHERS THEN ... RAISE NOTICE` (engole
  #     falha; causa do REC-ARTDB-03) FORA da allowlist; log+re-raise é aceito.
  #   - WARN (não-fail) em timestamp futuro no nome (padrão da casa: versão futura
  #     para ordenar migrations — AG-EX-14).
  # Allowlist = dívida histórica documentada (3 migrations de 2026-08-04; a 4ª do
  # achado A, campanhas_nps_cron.sql, foi CORRIGIDA em AG-EX-13 com RAISE EXCEPTION).
  migration-gates:
    name: Migration gates (antipadrão + timestamp)
    # ubuntu-latest (auditoria 2026-08-21, batch 3): regex local sobre migrations
    # commitadas, sem secrets/DB.
    runs-on: ubuntu-latest
    needs: lockfile
    steps:
      - name: Checkout code
        uses: actions/checkout@v7
        with:
          persist-credentials: false

      - name: Setup Node
        uses: actions/setup-node@v7
        with:
          node-version: 22

      - name: Run migration gates (allowlist de dívida histórica)
        run: node scripts/check-migration-gates.mjs --allowlist=20260804000000_canonical_schema_squash_133_migrations.sql,20260804150000_fix_secdef_revoke_extended_schemas.sql,20260804170000_fix_rls_systematic_coverage.sql

  test:
    name: Unit tests
    # ubuntu-latest (auditoria 2026-08-21, batch 3): bun run test usa só as envs
    # fake do topo do arquivo (VITE_SUPABASE_URL: https://example.supabase.co),
    # sem DB real.
    runs-on: ubuntu-latest
    needs: [lockfile, quality, audit-contract-scan, contract-gate, migration-gates]
    steps:
      - name: Checkout code
        uses: actions/checkout@v7
        with:
          persist-credentials: false

      - name: Setup Node
        uses: actions/setup-node@v7
        with:
          node-version: 22

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.14

      - name: Cache Bun dependencies
        uses: actions/cache@v6
        with:
          path: ~/.bun/install/cache
          key: bun-${{ runner.os }}-${{ hashFiles('bun.lock') }}
          restore-keys: |
            bun-${{ runner.os }}-

      - name: Install dependencies
        run: bun install

      - name: Chat UI unit tests (feat/chat-ui-100 gate)
        # E94 — subset rápido dos testes do chat para feedback rápido no PR
        env:
          NODE_OPTIONS: --max-old-space-size=6144
        run: bun run test:chat

      - name: Unit tests (blocking)
        # Stable after quarantining the infinite-loop useMessages test and fixing
        # the flaky waitFor>testTimeout mismatch: 2088 passed / 0 failed, no OOM,
        # verified locally including the coverage pass. Hard gate.
        env:
          NODE_OPTIONS: --max-old-space-size=6144
        run: bun run test -- --coverage --reporter=verbose

      - name: Upload coverage report
        uses: actions/upload-artifact@v7
        if: always()
        with:
          name: coverage-report
          path: coverage/
          retention-days: 7

      - name: Coverage threshold gate (ratchet)
        if: always()
        run: |
          SUMMARY="coverage/coverage-summary.json"
          if [ ! -f "$SUMMARY" ]; then
            echo "::warning title=Coverage gate::coverage-summary.json not found — skipping threshold check."
            exit 0
          fi
          node - <<'JSEOF'
          const fs = require('fs');
          const s = JSON.parse(fs.readFileSync('coverage/coverage-summary.json','utf8')).total;
          const floors = { lines: 25, functions: 18, branches: 15, statements: 24 };
          let fail = false;
          for (const [k, floor] of Object.entries(floors)) {
            const pct = s[k]?.pct ?? 0;
            const status = pct >= floor ? '✅' : '❌';
            console.log(`${status} ${k}: ${pct.toFixed(1)}% (floor ${floor}%)`);
            if (pct < floor) fail = true;
          }
          if (fail) { console.error('\n::error title=Coverage gate::One or more metrics are below the coverage floor. Raise coverage or adjust the floor intentionally.'); process.exit(1); }
          else { console.log('\nCoverage gate passed.'); }
          JSEOF

  build:
    name: Build
    # ubuntu-latest (auditoria 2026-08-21, batch 3): bun run build puro, sem
    # DB/VPS. O histórico de OOM abaixo é RAM real do HOST self-hosted (6144MB
    # não sustentou — "6144 pediu mais do que o disponivel"); ubuntu-latest tem
    # ~7GB dedicados, folga real sobre o que o self-hosted já não aguentava.
    runs-on: ubuntu-latest
    needs: [lockfile, quality, audit-contract-scan, contract-gate, migration-gates]
    steps:
      - name: Checkout code
        uses: actions/checkout@v7
        with:
          persist-credentials: false

      - name: Setup Node
        uses: actions/setup-node@v7
        with:
          node-version: 22

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.14

      - name: Cache Bun dependencies
        uses: actions/cache@v6
        with:
          path: ~/.bun/install/cache
          key: bun-${{ runner.os }}-${{ hashFiles('bun.lock') }}
          restore-keys: |
            bun-${{ runner.os }}-

      - name: Install dependencies
        run: bun install

      - name: Build for production
        # Historico de OOM neste job (2026-08-21):
        #   run 32443579939 sem NODE_OPTIONS: V8 "JavaScript heap out of
        #     memory" aos ~100s de transform (heap default ~1GB nesse runner).
        #   run 32455497422 com 6144: passou do transform (6403 modulos) e
        #     morreu SIGKILL durante "rendering chunks" — nao e mais o soft
        #     limit do V8, e o OOM-killer do kernel matando por falta de RAM
        #     real do container/host. 6144 pediu mais do que o disponivel.
        # 4096 fica entre os dois pontos medidos: acima do que sozinho ja
        # nao bastava, com folga abaixo do que o host nao sustentou.
        env:
          NODE_OPTIONS: --max-old-space-size=4096
        run: bun run build

      - name: Upload build artifacts
        uses: actions/upload-artifact@v7
        with:
          name: dist
          path: dist/
          retention-days: 7

      - name: Report bundle size
        run: |
          echo "Bundle Size Report"
          echo "=================="
          du -sh dist/
          echo ""
          echo "JS Files:"
          find dist -name "*.js" -exec du -h {} \;
          echo ""
          echo "CSS Files:"
          find dist -name "*.css" -exec du -h {} \;

  e2e:
    name: E2E tests
    # ubuntu-latest (auditoria 2026-08-21, batch 3): playwright.config.ts sobe
    # SEU PRÓPRIO `vite --port 5173` local (baseURL http://localhost:5173,
    # Supabase fake) — nunca toca a VPS real. O passo "Install Playwright
    # Browsers" já reinstala chromium do zero, então não depende do cache do
    # runner self-hosted. A premissa "repo privado + cota esgotada" de
    # 2026-08-18 não vale mais (repo público, ubuntu-latest gratuito). O
    # timeout de 60min abaixo já documenta que o vps-zapp estava sob
    # "contencao" do pool compartilhado — migrar remove essa causa raiz.
    runs-on: ubuntu-latest
    needs: build
    # 15 nao bastou (run 32466586329, 2026-08-21): job ficou 20min (09:19:06
    # -> 09:39:07) e foi cancelado pelo proprio timeout. Subi p/ 30 — TAMBEM
    # nao bastou: run 32471127022 (commit a7b6360, mesmo dia) rodou o step
    # "Run E2E tests" por 29min sem terminar a suite e foi cancelado exatamente
    # aos 30min de job (10:43:15 -> 11:13:26). Duas tentativas, dois valores
    # insuficientes — o pool de runners compartilhado (contencao documentada
    # na nota 11/12 do PR #1354) tem variancia alta demais pra cravar um
    # numero justo. 60min da 2x de folga sobre o pior caso ja observado (29min)
    # sem mascarar um hang real (a suite tem 13 specs, historico pre-contencao
    # e bem mais rapido que isso).
    timeout-minutes: 60
    steps:
      - name: Checkout code
        uses: actions/checkout@v7
        with:
          persist-credentials: false

      - name: Setup Node
        uses: actions/setup-node@v7
        with:
          node-version: 22

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.14

      - name: Cache Bun dependencies
        uses: actions/cache@v6
        with:
          path: ~/.bun/install/cache
          key: bun-${{ runner.os }}-${{ hashFiles('bun.lock') }}
          restore-keys: |
            bun-${{ runner.os }}-

      - name: Install dependencies
        run: bun install

      - name: Install Playwright Browsers
        run: bunx playwright install --with-deps

      - name: Run E2E tests (boot suite — src/tests/e2e, 13 specs)
        run: bun run test:e2e:boot

      - name: Upload Playwright report
        uses: actions/upload-artifact@v7
        if: always()
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 7

  a11y:
    name: Accessibility regression (axe)
    # ubuntu-latest (auditoria 2026-08-21, batch 3): mesmo motivo do job `e2e`
    # acima — playwright.a11y.config.ts também sobe seu próprio vite local
    # (sem E2E_BASE_URL/credenciais, cai no branch de Supabase fake, só o
    # project `public` roda), sem tocar a VPS real.
    runs-on: ubuntu-latest
    needs: build
    # 15 nao bastou (run 32466586329, 2026-08-21) — mesmo timeout/mesma causa
    # do job `e2e` acima (pool de runners sob contencao); ver comentario la.
    timeout-minutes: 30
    steps:
      - name: Checkout code
        uses: actions/checkout@v7
        with:
          persist-credentials: false

      - name: Setup Node
        uses: actions/setup-node@v7
        with:
          node-version: 22

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.14

      - name: Cache Bun dependencies
        uses: actions/cache@v6
        with:
          path: ~/.bun/install/cache
          key: bun-${{ runner.os }}-${{ hashFiles('bun.lock') }}
          restore-keys: |
            bun-${{ runner.os }}-

      - name: Install dependencies
        run: bun install

      - name: Install Playwright Browsers
        run: bunx playwright install --with-deps chromium

      - name: Run axe regression suite (/auth, /forgot-password, /reset-password + keyboard nav)
        run: bun run test:a11y

      - name: Upload a11y Playwright report
        uses: actions/upload-artifact@v7
        if: always()
        with:
          name: playwright-report-a11y
          path: playwright-report-a11y/
          retention-days: 7


  security:
    name: Security audit
    # ubuntu-latest (auditoria 2026-08-21, batch 3): bun audit (registro npm
    # público) + grep local por padrões de secret em src/, sem DB/VPS.
    runs-on: ubuntu-latest
    needs: lockfile
    steps:
      - name: Checkout code
        uses: actions/checkout@v7
        with:
          persist-credentials: false

      - name: Setup Node
        uses: actions/setup-node@v7
        with:
          node-version: 22

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.14

      - name: Cache Bun dependencies
        uses: actions/cache@v6
        with:
          path: ~/.bun/install/cache
          key: bun-${{ runner.os }}-${{ hashFiles('bun.lock') }}
          restore-keys: |
            bun-${{ runner.os }}-

      - name: Install dependencies
        run: bun install

      - name: Dependency audit (blocking on CRITICAL)
        run: |
          set +e
          audit_output=$(bun audit --audit-level=critical 2>&1)
          status=$?
          set -e
          echo "$audit_output"
          if [ "$status" -ne 0 ]; then
            if echo "$audit_output" | grep -q "audit request failed"; then
              echo "::warning title=Dependency audit::npm audit registry unreachable (transient 5xx). Skipping — no CVE action needed."
            else
              echo "::error title=Dependency audit::CRITICAL severity vulnerabilities found. Pin or replace the offending packages before merging."
              exit "$status"
            fi
          else
            echo "Dependency audit passed — no CRITICAL CVEs found."
          fi

      - name: Check for obvious secrets in code
        run: |
          echo "Checking for potential secrets..."
          matches=$(grep -rn "SUPABASE_SERVICE_ROLE_KEY\|sk_live_\|sk_test_\|password\s*=\s*[\"']" --include="*.ts" --include="*.tsx" --include="*.js" src/ || true)
          if [ -n "$matches" ]; then
            echo "$matches"
            echo "::warning title=Potential secrets::Potential secrets found. Review before release."
          fi



  # Gate 6 (pós-incidente 17/08): parse-check das edge functions.
  # main/index.ts NAO importa o grafo inteiro (~107 functions sao despachadas
  # em runtime via EdgeRuntime.userWorkers.create) — por isso o gate tem 2
  # camadas: (1) boot-check do edge-runtime com o main service (cobre o closure
  # do boot, incluindo _shared/contract-schemas.ts — a causa do incidente);
  # (2) parse de TODAS as functions via `deno cache` (parse + download de
  # imports, sem type-check — type errors de libs remotas nao bloqueiam).
  # Sem secrets — seguro em PRs de fork. Sem path filters (politica do ci.yml).
  edge-functions-parse-check:
    name: Gate 6 — edge functions parse-check (edge-runtime v1.74.0)
    # ubuntu-latest (auditoria 2026-08-21, batch 3): usa Docker, mas só
    # LOCALMENTE no próprio runner (docker run com SUPABASE_URL=http://localhost:8000
    # e credenciais dummy, boot-check + deno cache isolados) — zero acesso real
    # à VPS. ubuntu-latest já vem com Docker Engine pré-instalado.
    runs-on: ubuntu-latest
    needs: lockfile
    timeout-minutes: 15
    steps:
      - name: Checkout code
        uses: actions/checkout@v7
        with:
          persist-credentials: false

      - name: Pre-pull edge-runtime image
        run: docker pull supabase/edge-runtime:v1.74.0

      - name: Boot-check do main service (closure do boot)
        run: |
          set -euo pipefail
          LOG_FILE="$RUNNER_TEMP/edge-runtime-boot.log"
          # edge-runtime start é um servidor: em boot saudável ele fica de pé até
          # o timeout matar (124). Exit rápido != 124 = falha real (imagem/mount/
          # crash de boot). Erros de parse são caçados no log COMPLETO — sem head,
          # que truncaria a mensagem de erro e mataria o pipe com SIGPIPE (141).
          VOL="evo-gate6-$GITHUB_RUN_ID"
          docker volume create "$VOL" >/dev/null
          tar -C "$GITHUB_WORKSPACE/supabase/functions" -cf - . | docker run --rm -i -v "$VOL:/data" alpine:3.19 sh -c 'rm -rf /data/* /data/.* 2>/dev/null || true; tar -xf - -C /data'
          set +e
          timeout 90 docker run --rm \
            -v "$VOL:/home/deno/functions:ro" \
            -e SUPABASE_URL=http://localhost:8000 \
            -e SUPABASE_ANON_KEY=test-anon-key \
            -e SUPABASE_SERVICE_ROLE_KEY=test-service-role-key \
            -e JWT_SECRET=super-secret-jwt-token-with-at-least-32-characters-long \
            -e VERIFY_JWT=false \
            supabase/edge-runtime:v1.74.0 \
            start --main-service /home/deno/functions/main \
            >"$LOG_FILE" 2>&1
          status=$?
          set -e
          echo "edge-runtime exit code: $status (124 = timeout: runtime de pé até o fim da janela)"
          if [ "$status" -ne 0 ] && [ "$status" -ne 124 ]; then
            echo "::error title=Gate 6 (parse-check)::edge-runtime abortou no boot (exit $status)."
            sed -n '1,80p' "$LOG_FILE"
            exit 1
          fi
          ERRORS=$(grep -E "could not be parsed|worker boot error" "$LOG_FILE" || true)
          if [ -n "$ERRORS" ]; then
            echo "::error title=Gate 6 (parse-check)::Erro de parse/boot detectado nas edge functions:"
            echo "$ERRORS"
            exit 1
          fi
          echo "Gate 6 OK: boot-check do main (closure do boot) sem erros de parse."
          docker volume rm "$VOL" >/dev/null 2>&1 || true

      - name: Parse-check de TODAS as functions (fora do grafo do main)
        run: |
          set -euo pipefail
          cd "$GITHUB_WORKSPACE"
          COUNT=$(find supabase/functions -name '*.ts' -not -path '*/__tests__/*' -not -name '*.spec.ts' -not -name '*.test.ts' | wc -l)
          echo "Parse de $COUNT arquivos .ts (deno cache, sem type-check)"
          VOL="evo-deno-$GITHUB_RUN_ID"
          docker volume create "$VOL" >/dev/null
          trap 'docker volume rm "$VOL" >/dev/null 2>&1 || true' EXIT
          tar -C "$GITHUB_WORKSPACE/supabase/functions" -cf - . | docker run --rm -i -v "$VOL:/data" alpine:3.19 sh -c 'rm -rf /data/* /data/.* 2>/dev/null || true; tar -xf - -C /data'
          for attempt in 1 2; do
            set +e
            timeout 240 docker run --rm \
              -v "$VOL:/home/deno/functions:ro" \
              denoland/deno:2.2.8 cache --no-config --no-lock \
              $(find supabase/functions -name '*.ts' -not -path '*/__tests__/*' -not -name '*.spec.ts' -not -name '*.test.ts' | sed 's|^supabase/functions/|/home/deno/functions/|') \
              >"$RUNNER_TEMP/deno-cache.log" 2>&1
            rc=$?
            set -e
            if [ "$rc" -eq 0 ]; then
              echo "Gate 6 OK: todas as functions parsearam."
              exit 0
            fi
            if grep -qE 'file:///home/deno/functions/' "$RUNNER_TEMP/deno-cache.log"; then
              echo "::error title=Gate 6 (parse-check)::Parse error em edge function:"
              grep -E 'file:///home/deno/functions/' "$RUNNER_TEMP/deno-cache.log" | head -20
              exit 1
            fi
            echo "Tentativa $attempt falhou (rc=$rc) — provável flake de rede; log:"
            tail -20 "$RUNNER_TEMP/deno-cache.log"
          done
          echo "::error title=Gate 6 (parse-check)::deno cache falhou 2x (rede/resolução) — investigar antes de mergear."
          exit 1
