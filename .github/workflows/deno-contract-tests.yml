name: 🦕 Deno Contract Tests — Edge Functions

on:
  push:
    branches: [main]
    paths:
      - 'supabase/functions/**'
      - 'deno.json'
      - '.github/workflows/deno-contract-tests.yml'
  pull_request:
  # Sem paths filter em pull_request — o job check-paths decide se roda os testes
  # (corrige misconfig: PR só-.md ficavam bloqueados pois o check nunca reportava)
  # Varredura diária: main pode ficar vermelho sem PR tocando functions
  # (auditoria A6 2026-08-06 — ex.: onda de remoção #922 deixou testes
  # órfaos lendo index.ts deletados e ninguém viu até o próximo push).
  schedule:
    - cron: '0 6 * * *'
  # Bloco 1 (2026-08-21): dispatch manual — permite validar o fix do
  # runner/concurrency num branch antes de mergear, e rodar sob demanda.
  workflow_dispatch: {}

permissions:
  contents: read
  checks: write
  actions: write  # necessário p/ actions/cache@v6 SAVAR (sem isso: "Caching is skipped")

# Bloco 1 (2026-08-21, PLANO-100-CONTRATOS-EDGE): o group antigo usava só
# `github.ref`, que é `refs/heads/main` para TODO push nesse branch — em dias
# de merges frequentes (comum neste repo), cada push cancelava a run do push
# anterior antes de concluir, e o gate nunca fechava em `main` (runs
# `cancelled` em série; última conclusão REAL foi `failure` em 2026-08-20).
# Chave por PR (cancela run obsoleta da MESMA PR, comportamento desejado) OU
# por commit sha (push/schedule — cada push tem seu próprio grupo, nunca
# cancela o de outro). cancel-in-progress só se aplica a pull_request.
concurrency:
  group: ${{ github.workflow }}-${{ github.event_name == 'pull_request' && github.event.pull_request.number || github.sha }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  check-paths:
    name: Check paths (PR only)
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    outputs:
      should-run: ${{ steps.check.outputs.should-run }}
    steps:
      - uses: actions/checkout@v7
      - id: check
        run: |
          set -euo pipefail
          git fetch origin main
          CHANGED=$(git diff --name-only origin/main...HEAD | grep -E '^(supabase/functions/|deno\.json$)' || true)
          if [ -n "$CHANGED" ]; then
            echo "Paths touched — running contract tests"
            echo "should-run=true" >> $GITHUB_OUTPUT
          else
            echo "No contract test paths touched — skipping"
            echo "should-run=false" >> $GITHUB_OUTPUT
          fi
      - uses: actions/github-script@v7
        if: steps.check.outputs.should-run == 'false'
        with:
          script: |
            github.rest.checks.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              name: 'Contract Tests (Deno)',
              head_sha: context.payload.pull_request.head.sha,
              status: 'completed',
              conclusion: 'success',
              output: {
                title: 'Skipped — no contract test paths touched',
                summary: 'No files matching `supabase/functions/**` or `deno.json` changed in this PR.',
                text: 'Contract tests only run when edge functions or Deno config are modified.'
              }
            })

  deno-contract-tests:
    name: Contract Tests (Deno)
    needs: check-paths
    if: always() && (needs.check-paths.result == 'success' && needs.check-paths.outputs.should-run == 'true' || github.event_name != 'pull_request')
    # Bloco 1 (2026-08-21): label tinha "/vps-zapp" (barra colada) — os
    # runners self-hosted reais têm o label "vps-zapp" (sem barra, confirmado
    # via `gh api .../actions/runners`). O label errado nunca casava com
    # nenhum runner: as 3 runs mais recentes ficaram presas em `queued` sem
    # nunca despachar, o que também explica os `cancelled` em série (cada
    # push cancelava a anterior, que nunca tinha sequer começado a rodar).
    runs-on: [Linux, X64, vps-zapp]
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v7
        with:
          persist-credentials: false

      - name: Setup Deno
        uses: denoland/setup-deno@v2
        with:
          deno-version: v2.x

      - name: Cache Deno dependencies
        uses: actions/cache@v6
        with:
          # Deno v2 (Linux): DENO_DIR default = ~/.cache/deno (doc oficial).
          # O path antigo ~/.deno só tinha o fallback do setup-deno e o cache
          # deno-* nunca foi salvo (sem actions: write + path errado).
          path: ~/.cache/deno
          key: deno-${{ runner.os }}-${{ hashFiles('deno.json') }}
          restore-keys: |
            deno-${{ runner.os }}-

      - name: Lint no-direct-evo-url (report-only)
        run: |
          deno lint -c deno.json --rules-tags= --rules-include=no-direct-evo-url/no-direct-evo-url supabase/functions > /tmp/lint-out.txt || true
          cat /tmp/lint-out.txt
          if grep -q 'no-direct-evo-url' /tmp/lint-out.txt; then
            echo '::warning::no-direct-evo-url: violacoes encontradas (report-only - fase 2 torna blocking)'
          else
            echo 'no-direct-evo-url: 0 violacoes'
          fi
      - name: Run contract tests
        # Loop por arquivo (isolamento de processo): testes que setam env no
        # escopo de módulo (e2e-send.test.ts) e testes com fetch live
        # (evolution-api/index.test.ts, gmail-tests.test.ts) poluem/leakam no
        # modo invocação-única — cada arquivo em processo pró0rio isola isso.
        # Sem --no-check: type-check faz parte do contrato (TS-2339 derruba).
        run: |
          set -euo pipefail
          TEST_FILES=$(find supabase/functions -name '*.test.ts' -type f 2>/dev/null || true)
          if [ -z "$TEST_FILES" ]; then
            echo "::warning::No contract test files found — skipping"
            exit 0
          fi
          COUNT=$(echo "$TEST_FILES" | wc -l)
          echo "Found $COUNT contract test files"
          echo "$TEST_FILES" | while IFS= read -r f; do
            echo "::group::Testing $f"
            deno test --allow-net --allow-env --allow-read "$f" || {
              echo "::error title=Contract test failed::$f"
              exit 1
            }
            echo "::endgroup::"
          done
        env:
          SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
          SUPABASE_ANON_KEY: ${{ secrets.VITE_SUPABASE_PUBLISHABLE_KEY }}
