# ============================================
# ZAPP-WEB .gitignore - Segurança Primeiro!
# ============================================

# ====================
# SECRETS E AMBIENTE (CRÍTICO!)
# ====================
.env
.env.*
.env.local
.env.development
.env.development.local
.env.test
.env.test.local
.env.production
.env.production.local
.env.staging
*.pem
*.key
*.crt
*.p12
secrets/
.secrets
credentials.json
service-account.json
firebase-adminsdk*.json

# Credenciais em texto plano (PT-BR e EN)
*credencia*.txt
*credential*.txt
*cred*.txt
*token*.txt
*apikey*.txt
*api_key*.txt
*senha*.txt
*password*.txt
*secret*.txt
*chave*.txt

# ====================
# CONFIGURAÇÕES MCP (CRÍTICO - CONTÉM TOKENS!)
# ====================
# O arquivo .mcp.json contém URLs autenticadas com tokens embutidos.
# NUNCA commitar. Usar .mcp.json.example como referência.
.mcp.json
!.mcp.json.example
.mcp.json.bak
.mcp.json.backup
.mcp.local.json
*.mcp.local.json

# ====================
# LOGS
# ====================
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
.pnpm-debug.log*

# ====================
# DEPENDÊNCIAS
# ====================
# AUDITORIA 2026-07-17: era "node_modules/" (com barra), que ignora apenas
# DIRETORIO. Um symlink chamado node_modules e um ARQUIVO -> passava batido no
# git add -A, era commitado, e no runner virava symlink pendurado. bun install
# morria com: ENOENT: could not open the "node_modules" directory (exit 1).
# Sem a barra, ignora diretorio, symlink e arquivo.
node_modules
.pnp
.pnp.js
.yarn/install-state.gz

# ====================
# LOCKFILES
# ====================
# Este projeto usa BUN como gerenciador de pacotes.
# O arquivo canônico é bun.lock (commitado e rastreado).
# package-lock.json é gerado pelo npm e NÃO deve ser commitado
# (diverge do bun.lock e causa installs não-determinísticos em CI).
# Para instalar: bun install
package-lock.json
# bun.lockb é o formato binário do bun v1 (este projeto usa bun v1.3+ texto)
bun.lockb

# ====================
# BUILD E PRODUÇÃO
# ====================
dist/
dist-ssr/
build/
.next/
out/
*.local

# ====================
# CACHE
# ====================
.cache/
.parcel-cache/
.turbo/
.vercel/
.netlify/
.vite/
*.tsbuildinfo

# Deno+Vite shadow cache (regenerado por bun install, causa crash do esbuild no Windows)
node_modules/.deno/

# ====================
# TESTES
# ====================
coverage/
.nyc_output/
test-results/
playwright-report/
playwright-report-a11y/
playwright/.cache/
pw-output/

# ====================
# EDITOR E IDE
# ====================
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea/
*.sublime-workspace
*.sublime-project
.project
.classpath
.settings/

# ====================
# SISTEMA OPERACIONAL
# ====================
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
*.swp
*.swo
*~

# ====================
# SUPABASE LOCAL
# ====================
supabase/.branches/
supabase/.temp/
supabase/functions/_test/
.supabase/

# ====================
# ARQUIVOS GRANDES
# ====================
*.zip
*.tar.gz
*.rar
*.7z

# ====================
# BACKUPS
# ====================
*.bak
*.backup
*_backup*
*_old*
*.orig

# ====================
# OUTROS
# ====================
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.eslintcache
.stylelintcache
*.tgz
.yarn-integrity

# Playwright auth state
e2e/.auth/
playwright-report-e2e/
.dist-backups/

# ====================
# ARTEFATOS TEMPORÁRIOS DE DEV
# (Não são código de aplicação)
# ====================
# Diretório de trabalho temporário
tmp/
scripts/tmp_queries/

# Saídas de scripts de auditoria ad-hoc
audit.js
audit.mjs
audit.json
full_audit_report.txt

# Rastreamento de deps desatualizadas (muda toda semana, não é fonte da verdade)
outdated.txt

# Scripts de dados mock e saídas SQL grandes
generate_mock_data.py
mock_data.sql

# Saída de build do TypeScript (gerado por `tsc --noEmit 2> ts_errors.txt`)
# Não versionar: muda a cada build, aparece vazio quando clean
ts_errors.txt

# Artefatos de auditoria de design gerados (não são código-fonte)
design-system-audit.html
design-system-audit.md

# Relatórios de CI gerados por check-types-schemas.mjs e scripts similares
# /reports/ (root only) to avoid matching src/components/reports/
/reports/
# exceção: snapshot semanal de schema commitado pelo CI (schema-snapshot.yml)
!/reports/
!/reports/schema-snapshot.sql

# Artefatos históricos de sessões/auditorias (movidos para docs/history/)
ROUND-15-*.md
ROUND-17-*.md
MIGRATION-*-EXECUTION-READY.md
MIGRATION-EXECUTION-PLAN.md
EXECUTION-SIMULATION-*.md
EXHAUSTIVE_VALIDATION_REPORT.md
DEPLOYMENT-READINESS-CERTIFICATE.md
DEPLOYMENT_EXECUTION_STATUS.md
PRODUCTION_EXCELLENCE_IMPLEMENTATION.md
PERFORMANCE-SLA-VALIDATION.md
RUNTIME-RLS-VALIDATION.md
SCHEMA-INTROSPECTION-PROTECTION.md
FALHAS_E_GAPS.md
FINAL_CHECKLIST.txt
IMPLEMENTATION_SUMMARY.txt
SMOKE-TESTS-ROUND15.sql
ROUND-15-EXHAUSTIVE-TEST-SUITE.sql
.gitignore_mcp_patch


# ====================
# BLOBS GRANDES — DADOS, NÃO CÓDIGO (adicionado pela auditoria Hermes 2026-07-28)
# ====================
# Arquivos de dados Lalamove — 23+ MB de JSON, PDF, HTML, docx
lalamove_*
Credencias*
Credenciais*
deno.lock
graphify-out/*
!graphify-out/GRAPH_REPORT.md
!graphify-out/manifest.json
!graphify-out/.graphify_labels.json
!graphify-out/.graphify_labels.json.sig

.vercel
.env*
!supabase/functions/.env.required
!supabase/functions/.deployignore
__pycache__/
*.pyc

# ====================
# AGENTES AI / ECC TOOLS (gerados por bots — NUNCA versionar)
# ====================
# ECC Tools bot gerou 11 arquivos em 21/07/2026 (.agents/, .codex/, .claude/)
# e foram removidos em 0873919e3 por serem código morto (score 0/7, info
# incorreta, redundantes com CLAUDE.md e AGENTS.md versionados na raiz).
# Claude Code (`claude /init`), Codex CLI (`codex`) e o ECC Tools podem
# recriar essas pastas na raiz a qualquer momento — sem estas regras, um
# `git add -A` acidental os reintroduziria (mesmo incidente do node_modules).
.claude/
.agents/
.codex/
.hermes/
.hermes-chats/

# Artefatos de diagnóstico local
br.gz
buildrecord/
dev/
evo_exec.json
scripts/preview/design-*.png
scripts/preview/logs/
docs/ops/_scratch/
src/integrations/supabase/types.ts.new
